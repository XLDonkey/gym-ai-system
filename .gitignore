# ── Python ─────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.so
*.egg
*.egg-info/
dist/
build/
MANIFEST

# ── Virtual environments ────────────────────────────────────────────────────────
venv/
env/
ENV/
.venv/
.env/
xlf-env/

# ── Jupyter ─────────────────────────────────────────────────────────────────────
.ipynb_checkpoints/

# ── ML model weights (store in Google Drive / release assets, not git) ─────────
models/weights/*.pt
models/weights/*.pth
models/weights/*.onnx
models/weights/*.hef
models/weights/*.tflite
models/weights/*.h5
models/weights/*.pb
models/weights/*.keras
*.pt
*.pth
*.onnx
*.hef
*.tflite
*.h5
*.pb
# Exception: keep browser demo model
!pose/model.json
# Exception: keep versioned model metadata JSON
!models/registry.json

# ── Training data (too large for git — use Google Drive) ───────────────────────
data/raw/
data/processed/
data/videos/
*.mp4
*.mov
*.MOV
*.avi
*.mkv
*.webm
*_sequences.json
*_angles.json
training_angles.json
# Annotation exports are intentionally NOT ignored — commit them

# ── Logs & experiment tracking ──────────────────────────────────────────────────
logs/
runs/
wandb/
mlruns/
*.log
lightning_logs/
checkpoints/

# ── Secrets & credentials ────────────────────────────────────────────────────────
.env
.env.*
*.env
secrets.json
credentials.json
*.pem
*.key
*.token
rclone.conf
pi/rclone.conf

# ── OS ──────────────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Node ────────────────────────────────────────────────────────────────────────
node_modules/
.next/

# ── IDE ──────────────────────────────────────────────────────────────────────────
.vscode/
.idea/

# ── Temp ────────────────────────────────────────────────────────────────────────
*.tmp
*.bak
*.swp
*~
